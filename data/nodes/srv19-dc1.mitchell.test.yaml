# Windows Firewall Rules
windows::firewall::individual_firewall_rules:
    'NTP':
      description:               "Managed by Puppet -- Allows Time Sync using NTP"
      local_port:                "123"
      protocol:                  "udp"
